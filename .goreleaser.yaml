version: 2

project_name: FusionDataCLI

before:
  hooks:
    # Build the embedded web UI before any go build so server/webdist ships
    # the current React/MUI bundle in release binaries.
    - make web
    - go mod tidy

builds:
  - id: fusiondatacli
    main: .
    binary: fusiondatacli
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X github.com/schneik80/FusionDataCLI/config.DefaultClientID={{ .Env.APS_CLIENT_ID }}
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: fusiondatacli
    name_template: "{{ .ProjectName }}-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE
      - README.md

checksum:
  name_template: "checksums.txt"

brews:
  - name: fusiondatacli
    repository:
      owner: schneik80
      name: homebrew-fusiondatacli
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/schneik80/FusionDataCLI"
    description: "Terminal browser for Autodesk Platform Services Manufacturing Data Model"
    license: "GPL-3.0"
    install: |
      bin.install "fusiondatacli"
    commit_author:
      name: goreleaser
      email: goreleaser@users.noreply.github.com
    commit_msg_template: "chore: brew formula update for {{ .ProjectName }} {{ .Tag }}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: schneik80
    name: FusionDataCLI
  name_template: "v{{ .Version }}"
  draft: false
  prerelease: auto
  footer: |
    ## Install

    **Homebrew (macOS / Linux)**
    ```
    brew install schneik80/fusiondatacli/fusiondatacli
    ```

    **macOS (Apple Silicon)**
    ```
    curl -L https://github.com/schneik80/FusionDataCLI/releases/download/v{{ .Version }}/FusionDataCLI-{{ .Version }}-darwin-arm64.tar.gz | tar xz
    sudo mv fusiondatacli /usr/local/bin/
    ```

    **macOS (Intel)**
    ```
    curl -L https://github.com/schneik80/FusionDataCLI/releases/download/v{{ .Version }}/FusionDataCLI-{{ .Version }}-darwin-amd64.tar.gz | tar xz
    sudo mv fusiondatacli /usr/local/bin/
    ```

    **Linux**
    ```
    curl -L https://github.com/schneik80/FusionDataCLI/releases/download/v{{ .Version }}/FusionDataCLI-{{ .Version }}-linux-amd64.tar.gz | tar xz
    sudo mv fusiondatacli /usr/local/bin/
    ```
